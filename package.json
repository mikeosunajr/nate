{
  "name": "nate",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "binaryen": "^105.0.0",
    "esbuild": "^0.14.18"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --sourcemap --outdir=dist --platform=browser --format=esm",
    "build:node": "esbuild src/index.ts --bundle --sourcemap --outdir=dist --platform=node --format=esm"
  },
  "devDependencies": {
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "browser": {
    "binaryen": "./binaryen.js"
  }
}
